# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~


POST    /users                      controllers.UserController.registerUser

GET     /users                      controllers.UserController.getUserByEmail(email: String)
